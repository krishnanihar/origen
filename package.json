{
  "name": "origen",
  "private": true,
  "version": "0.1.0",
  "description": "MCP-native design system",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "tokens:build": "pnpm --filter @origen/tokens build",
    "mcp:dev": "pnpm --filter @origen/mcp dev"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.0.0"
}
